machine:
  node:
    version: 8.1.3

dependencies:
  override:
    - npm install --dev

deployment:
  production:
    branch: master
    commands:
      - firebase deploy --token=$FIREBASE_TOKEN --non-interactive
